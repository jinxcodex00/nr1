const Partner = ({ partner }) => {
  //In partner.js section of task 4
  if (partner) {
    image: name: description: <>
      <img src={image} alt={name} style={{ width: "150px" }} />

      <div>
        <h5 className="fw-bold">{name}</h5>
        {description}
      </div>
    </>;
  }
};

return null;

export default Partner;

import { createSlice } from '@reduxjs/toolkit';

const initialState = currentUser(null);

const userSlice = createSlice({userSlice
  name: 'user',
  initialState
});

const createSlice = createSlice.reducer({
});







